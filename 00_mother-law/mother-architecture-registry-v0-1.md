# Mother Architecture Registry v0.1 — Historical Compatibility Projection

## Status
HISTORICAL_ONLY / READER_DISABLED / ROUTING_DISABLED / AUTHORITY_NONE

本檔保留的是早期 DCP／CoreTri／Mother-law 分層與命名實驗的 lineage，不是現行 Registry、Current、Authority、Runtime Spine 或 Dispatch Surface。

舊版曾以 M0–M5、Freeze、archive-before-upgrade、Mother Source holding area、Candidate Module 等分類整理早期 DCP、DXS、XDDC、DXC、XDDL、XLEN、LingLuo、XuanLing、CoreTri 與 v1–v3 文本。這些分類曾有歷史用途，但不得因本檔仍存在而恢復成現行架構。

## Retained nutrients

- Source／revision／lineage 必須可追溯；名稱、載體、圖像、文件不自動取得 Identity 或 Authority。
- 早期內容若仍含唯一 Evidence，可在具名 conflict／re-entry／audit 需求下 bounded read。
- Projection、runtime carrier、external tool 與 Native meaning 必須分離。
- Unknown／missing source 維持 Unknown，不因後續方便補成 Current fact。
- Successor 已吸收的能力不再維持第二份平行控制面。

## Metabolized semantics

以下語義退出 Active Reader Route：

- `Mother Architecture Registry` 作中央 Registry。
- M0–M5 作現行全域分類法。
- `Freeze`／holding area／archive-before-upgrade 作預設生命周期。
- DXS／XDDC／DXC／XDDL／XLEN 等歷史名稱因曾出現而自動恢復為 Current module。
- `return_to_00` 作固定中央回流路徑。
- GitHub／AGENTS／schedule／window instruction 因可執行而取得 Runtime／Authority。

Git history 已保存本檔前身，因此不再複製一份 archive body。若沒有 unique unresolved evidence，本檔不得被 normal reader 展開為工作來源。

## Current interpretation boundary

現行工作只接受 Native Current／Pointer、receiver-owned disposition、material return／receipt 與 affected bounded evidence。Carrier／repo／folder／file title 僅是 capability surface。

本檔若被命中：

1. 先判是否存在具名 unresolved lineage／evidence need。
2. 無 Material Need → `HISTORICAL_ONLY / NO_REENTRY`。
3. 有唯一 evidence need → 只讀 affected slice，回到真正 Native Receiver。
4. 不建立第二 Registry、第二 Current、第二 Mother、第二 DCP。

## Lifecycle

legacy_body_absorbed=true
normal_reader=false
wake=false
routing=false
authority=false
runtime=false
promotion=false

歷史不靠堆積保存；可泛化部分已進現行生命鏈，無剩餘作用的分類語義由 Git history 承接 lineage，讓 current surface 自然變輕。