# Repository Corpus Inventory Map

> Lifecycle: `HISTORICAL_REFERENCE`
> Current authority: `false`
> Runtime: `false`
> Native source root: `false`

This file is retained only as a historical redirect after the original inventory model was absorbed by the current-surface, disposition, successor, and semantic-core mechanisms.

## Current reader entry
1. `CURRENT-SURFACE-MANIFEST.json`
2. `REPOSITORY_CORPUS_INDEX.md` for lineage/discovery only
3. Private semantic-core disposition/successor evidence for bounded reclaim decisions

## Historical note
The former inventory classified many 01–05 families as `approved / keep` and prohibited cleanup. Those classifications are no longer authoritative. The detailed former inventory remains recoverable from Git history when provenance, audit, regression, or rebuild requires it; it must not be materialized into Current reasoning by default.

## Invariants
- `Inventory entry != Admission`
- `Repository path != Native Home`
- `Latest != Current`
- `Historical reference != executable capability`
- `Readable != retainable / mutable / write-back authorized`

## Reclaim rule
Physical reclaim is allowed only after successor, pointer/citation, authority/evidence, and re-entry/rebuild gates are satisfied. This redirect itself does not grant deletion authority for other artifacts.
