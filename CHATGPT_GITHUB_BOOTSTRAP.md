# Repository Re-entry Surface

> Lifecycle: `PUBLIC_RESEARCH_REFERENCE`
> Reader role: `REENTRY_ONLY`
> Normal reader priority: `false`
> Runtime: `false`
> Native source root: `false`
> Authority: `none`

Purpose: provide a minimal, low-read re-entry surface without treating GitHub, a repository name, or a historical map as system identity or Native Authority.

## Stable repository binding
- repository_id: `1125976757`
- repository_full_name: `chenchienheng/DCP-Pole-Projection`
- repository_lineage_aliases:
  - `chenchienheng/DCP-Framework`
  - `chenchienheng/XuanLing-00-Foundation-DCP`
- default_branch: `main`
- repository_role: `PUBLIC_DCP_POLE_PROJECTION_CARRIER`

The aliases resolve the same GitHub repository lineage. Name continuity does not transfer Native identity, authority, approval, or source ownership.

## Reader entry
This re-entry note does not own reader order. Resolve the exact repository-local order from `CURRENT-SURFACE-MANIFEST.json`, then read only the explicitly affected and re-admitted public surface.

Do not use file recency, root location, repository naming, search ranking, or historical cross-links to infer Current state or Native Authority.

## Re-entry rules
- verify actual connector and repository binding when repository work is required;
- treat this repository as a replaceable public carrier and reference surface;
- preserve stable repository identity with repository ID plus canonical name and lineage aliases;
- resolve lifecycle, evidence, dependency, return, reconciliation, and rebuild claims at the affected surface;
- re-materialize historical artifacts only for bounded provenance, audit, failure learning, regression, or rebuild.

## Not to claim
- repository access does not establish action authority;
- repository durability does not make GitHub a Native Source Root;
- successful write does not prove Runtime capability;
- public-safe content is not public-approved unless the applicable release gate says so;
- this re-entry surface is not Canon, Native Current, or a promotion decision.

## Historical compatibility
Earlier stage implementations used the two lineage aliases above, a GitHub-centric chain map, and GitHub as a primary writeback or structural-bone candidate. Their historical detail remains recoverable from Git history and the existing archive generation indexes; none is reactivated by this re-entry surface.
