# Naming Drift File-Level Diffs — Historical Comparison

> Lifecycle: `HISTORICAL_REFERENCE`
> Current execution eligibility: `false`
> Runtime: `false`
> Authority: `none`

This file records an earlier file-level comparison between underscore and hyphenated repository families. It does not instruct current readers to repopulate legacy families, move seed files, or write decisions into a cleanup register.

## Retained primitives
- file/path naming drift may hide or duplicate discovery paths;
- placeholders and actual files must be distinguished by evidence;
- before physical move/rewrite/retire, resolve Stable Existence, successor coverage, unique lineage/evidence and live caller/rebuild dependencies;
- naming convention is navigation metadata, not ontology.

## Retired execution sequence
The former `classify -> preserve/rewrite/retire -> move into canonical family -> cleanup-register writeback` sequence is not a current workflow. In particular, the existence of `01_runtime-spine`, `03_board-orchestration`, or `04_adapter-layer` does not require historical underscore content to be migrated into them.

`File-level difference != Required migration`
`Unique predecessor != Required live body`
`Canonical-looking path != Native Home`
`Cleanup register entry != Admission`

The exact former file lists and seed-branch observations remain recoverable in Git history for provenance or bounded reconstruction.
