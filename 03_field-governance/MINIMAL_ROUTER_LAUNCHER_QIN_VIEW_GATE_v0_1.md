# Minimal Router / QIN View Gate v0.1 — Metabolized Primitive Stub

**Lifecycle:** METABOLIZED_PRIMITIVE_STUB  
**Current eligibility as fixed router/QIN schema:** false  
**Runtime:** false

## 保留 Primitive
- Route success ≠ Content approval／Native absorption。
- Read／write route must preserve Domain、Authority、State、Source/Evidence、Affected Scope、Write Target、Return Target。
- Raw staging／unverified extraction／draft projection cannot be read as fact merely because a router can reach it。
- Mutation targets must be explicitly authorized; unavailable authority stops the affected branch rather than silently rerouting to a hard-coded platform。
- Router／UI／Agent is a Carrier/Execution surface, not Authority or Truth。

舊 BASE→Governed View→QIN、固定 table list、GitHub/Linear target 與 production DB prohibition matrix 只作 historical implementation specimen。

現行 successor：Boundary-first → six-dimensional dispatch → Effect Dependency → Gate → Action → Receiver-owned Return/Reconciliation。

完整舊 router schema 保留於 Git history。
