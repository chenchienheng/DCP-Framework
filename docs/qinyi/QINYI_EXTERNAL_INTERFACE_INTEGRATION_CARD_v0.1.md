# Qinyi External Interface Integration — Bounded Projection Reference

> Lifecycle: `PUBLIC_CANDIDATE_REFERENCE`
> Runtime: `false`
> Authority: `none`
> Public-approved: `false`

This file preserves a bounded external-interface projection associated with Qinyi. It does not establish Qinyi as the first, primary, canonical, or mandatory public route for XuanLing, and it does not grant publication or identity authority.

## Retained interface primitives
- external-facing material may require a public/private boundary;
- a public projection must not expose protected sources, private dependency chains, unreleased internal rules, or unauthorized personal material;
- visual/text/interface variants should back-map to an approved source identity or meaning anchor when identity continuity is material;
- external feedback may return as evidence/candidate delta, but return does not become Native absorption or architecture change by itself.

## Carrier neutrality
IG, GitHub, Zenodo, web pages, repositories, galleries, documents, videos, collaboration tools and future surfaces are optional carriers. None is a permanent public anchor, source of truth, or publication authority.

## Release boundary
`Public-safe != Public-approved`
`Interface projection != Identity source`
`Published artifact != Native body`
`External feedback != Architecture authority`

Any actual release requires the applicable owner/release authority and current public-boundary review.

## Historical topology retired
The former Mother-Architecture window, ScheduleHub, fixed content routes, fixed public anchors, tri-coupling operation view, and Qinyi-as-primary-conduit framing are historical implementation projections only. Their full predecessor body remains in Git history.
