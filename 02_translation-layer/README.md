# 02 Translation Layer — Legacy Family Stub

此資料夾是早期 Translation／LingLuo／Cloud-over-Cloud generation 的 lineage container，不是 Current architecture layer。

Current translation／handoff semantics 已由 `LIFECYCLE_DEPENDENCY_CHAIN_KERNEL.md` 的 Boundary、Evidence、Representation、Return／Reconciliation 與六維 dispatch 承接。

本 family 內舊 Body 若 successor coverage 完整且無獨立 evidence value，應從 active repository surface 排除；需要考古時使用 Git history。
