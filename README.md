# DCP Xuan-Ling CoreTri

Status: Candidate / Root Carrier / Not Approved Doctrine / No Runtime

## Core

This repository is the root carrier for DCP, XLEN, XAFD, XLQY, Open XuanLing Core, and CoreTri alignment.

It is not a runtime system, not a company policy, not a deployed agent, and not a public approval of any domain pack.

## Role

```yaml
Repo_Role:
  name: "DCP_Xuan-Ling_CoreTri"
  layer: "root / core / lineage"
  use_as:
    - "DCP / XLEN / XAFD / XLQY lineage"
    - "Open XuanLing Core candidate"
    - "Source / Carrier / Authority / Gate / Action / Return / Rebuild"
    - "State boundary"
    - "Red doors"
    - "PR / issue governance"
    - "domain-pack staging before split"
  do_not_use_as:
    - "runtime"
    - "company system"
    - "private family archive"
    - "customer-data carrier"
    - "approved doctrine"
```

## Invariant Chain

```text
Source -> Carrier -> Authority -> Gate -> Action -> Return -> Rebuild
```

## Related Carriers

```yaml
Related_Carriers:
  XLQY_Qinyi_Flow_CoreTri:
    role: "Qinyi flow, CoreTri, multi-carrier assistant mode, GUI Flow, return patterns"
  Yiyi_Xiao_shi_guang_CUI_App:
    role: "private small-operator guard app carrier; no customer data or credentials"
```

## Red Doors

- Candidate != Approved.
- BuildReady != Runtime.
- Return Packet != Closeout.
- Domain Pack != Open Core Requirement.
- Public-safe != Public-approved.
- Tool Capability != Permission.
- Repo Split != Doctrine Approval.

## Current Phase

```yaml
Current_Phase:
  name: "Pre-Stabilized Chain Integration"
  status: "Candidate / Under Review"
  active_work:
    - "PR #298 cleanup"
    - "Issue #299 dependency intake"
    - "Open Core extraction"
    - "domain pack staging"
```

## Final Rule

This repository holds the root governance grammar and staging material. It should keep the core small, domain packs optional, and private or operational material out of the public root carrier.
